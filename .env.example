# URL of the local LLM server (e.g., Ollama, llama.cpp)
# Leave empty to use the default fallback: http://localhost:8080/v1
NEXT_PUBLIC_LLAMA_SERVER_URL=http://localhost:11435

# Default model to use when no model is selected in settings (required)
NEXT_PUBLIC_DEFAULT_MODEL=unsloth/Qwen3.6-35B-A3B-GGUF:Q5_K_XL
